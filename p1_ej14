#include <iostream>
using namespace std;

int main() 
{
    int st, h, m, s;
    cout << "ingrese los segundos: ";
    cin >> st;
    h = st / 3600;
    st %= 3600; 
    m = st / 60;
    s = st % 60;
    cout << h << " horas, " << m << " minutos y " << s << " segundos." << endl;
    return 0;
}
